generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Movies {
  id       Int       @id
  title    String?
  director String?
  year     Int?      @db.SmallInt
  series   String?
  date     DateTime? @db.Date
  notes    String?
  mid      BigInt?
}
